# 11.4 — Correctness and A/B Gate: DEFERRED

## Status: N/A — No Attention TensorOps Prototype Exists to Gate

---

## Why This Task Is N/A

1. **Task 11.2 deferred** the attention TensorOps decision pending post-MoE
   profiling data from Apple Silicon hardware. No threshold determination was
   possible.

2. **Task 11.3 was conditional on 11.2 proceeding** — no attention TensorOps
   prototype was built.

3. **No new attention kernel exists** to pass through a correctness or A/B gate.
   There is nothing to measure, compare, or accept/reject.

4. **The existing attention kernel (FlashAttention) is unchanged.** It continues
   to use SIMD-group matrix operations and has its own established correctness
   coverage.

5. **The existing TensorOps indexer** (task 4, `kernel_dsv4_indexer_scores_nax`)
   already passed its own correctness gate and production policy lock (task 4.4).
   That path is separately validated and does not require re-gating here.

## Requirements Disposition

| Requirement | Status |
|-------------|--------|
| R12 (Correctness) | No new code to validate; existing paths retain their correctness guarantees |
| R13 (Benchmarks) | No new candidate to benchmark against baseline |
| R16 (Performance acceptance) | No candidate exists that could become default |

## Future Activation

When/if task 11.2 proceeds (Apple Silicon profiling shows attention >25% GPU time
post-MoE TensorOps), this gate becomes the acceptance checkpoint for any attention
TensorOps prototype:

- Correctness: bit-exact or bounded-error parity with current FlashAttention output
- A/B gate: repeatable end-to-end prefill improvement at realistic sequence lengths
- Reject if: microbenchmark win but end-to-end neutral/slower (per R16)

See: `dev-notes/11.2-11.3-attention-decision.md` for full decision rationale.
